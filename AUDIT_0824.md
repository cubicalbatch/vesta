# Vesta Code Audit — 2026-08-24

**Scope:** full repo at `56aff0b` (tree clean) — `src/vesta/**` (38,310 LOC Python), `frontend/src/**` (14,295), `tests/**` (26,594), `scripts/**` (5,356), 15 migrations, CI/packaging.
**Method:** fourteen parallel deep-review passes (retrieval · agent-chat · answer · api-routers · bench-api · eval-domain · storage/index/jobs · zim+catalog · inference+encoders · cli+config · frontend · security · 0822-fix-regression · aux/tests-CI), then orchestrator re-verification. An upstream model rate-limit interrupted partway: four passes delivered complete reports and two more delivered full summaries; after the limit cleared, every interrupted slice was re-run as a fresh serial deep pass and folded in — chat/agent-stream, answer-pipeline, frontend, cli+config, storage/index/jobs, aux/tests-CI, eval-domain, retrieval-core, and the 0822-remediation regression sweep all delivered complete findings (the zim+catalog re-run crashed twice mid-analysis, so its two findings were recovered from transcript and independently verified by the orchestrator). Every **Major** below and every carried-over status verdict was independently re-verified against HEAD code before inclusion (bench persistence and metric-math claims additionally reproduced empirically, marked **[emp]** where applicable).

**The headline correction:** the 0822 remediation (38 commits) covered the **Major, Performance, duplication, and dead-code tiers only**. The 0822 **Medium tier was never remediated** — of its ~55 items, roughly two thirds are still open at HEAD, several of them (B1, A1, A2, S1, S2, Z2) unchanged since v0.1. This audit therefore carries a verified per-item status table instead of re-arguing those findings.

Excluded by design (unchanged): no-auth trusted-LAN posture, off-by-default feature flags, `_WIDENED_CAPS`, the deprecated `vesta eval` alias, the deliberately reverted extract cache, and the documented 0822 follow-ups (PATCH-all-None 200, stale media/documents docstrings, word-set split).

---

## Executive summary

The 0822 remediation itself holds up: all ten Major fixes re-verified sound at their seams (M4 rank contract complete across all five emitters, M5 mirror truncation end-to-end correct, M6 migration wrapper structurally atomic, M7/M8 CLI arms correct, sanitizers survived a dedicated bypass hunt). The rot has moved to five places:

1. **Bench/eval persistence round-trips destroy data.** `vesta bench rejudge` rewrites every rejudged row's latency/rounds to zero and recomputes the run's headline latency percentiles as n=0 **[emp]**; `abort_reason`, `judge_shares_endpoint`, and eval `started_at` never survive their row mappers; failed and crashed eval runs report `done`.
2. **One failed bench cell abandons the matrix** — `asyncio.gather` without cancellation leaves sibling cells running detached, later flipping rows from `aborted` back to `complete` on a "failed" group; `DELETE /runs/{id}` during a run triggers the same cascade via an FK crash.
3. **Lifecycle seams the 0822 fixes didn't reach:** llama-server `stop()` races that leak the child (M3 incomplete), an index sidecar/wipe cross-arm hole (M8 incomplete), capability reseed gaps (S3 incomplete).
4. **Declared-but-dead controls:** the entire confidence-gate calibration feature (4 hot settings + `calibrate.py` + a CLI claim that the running app picks them up) is wired to nothing; the shipped **"How thorough" UI control plus eight `retrieval.*` settings are placebos** (M18); seven more placebo settings live in config/ (N21, incl. a placebo `server.auth.password`); six bench settings and two eval settings still read `.default`.
5. **Conversation persistence and driver divergence:** the chat route persists only the last sources *delta* (Round-0 cards vanish from restored conversations), and the non-streaming driver bench measures returns pre-recovery answers whenever cleanup is off — plus reset-before-replacement survives in three of four recovery arms (C5 was never fully fixed).

| Severity | Count |
|---|---|
| Major (new since 0822) | 18 |
| Medium (new since 0822) | 47 |
| 0822 Medium-tier items still open / partial | ~36 (status table below; L5/L10/S1 escalated to M12/M11/M15) |
| Dead-code items (new) | 33 |
| Duplication clusters (new/remaining) | 21 |

---

## Major findings (new)

### M1 · `bench rejudge` zeroes persisted latency metrics and rounds
`src/vesta/api/bench.py:1300-1335` (mapper) · `src/vesta/eval/bench_runner.py:899-937` (rejudge) · `src/vesta/api/bench.py:1121-1152` (rewrite)
`_row_to_result` omits `rounds` and `latency_ms` from the row→dataclass mapping — a row stored with rounds=3/latency=1234.5 reads back 0/0.0 **[emp]**. `rejudge_run` round-trips rows through this mapper twice with destructive effect: `update_question_result` rewrites the rejudged rows' columns with the zeros (original measurements lost from the DB), and `_rebuild_scored` builds every `ScoredQuestion` with `latency_ms=0`, so the recomputed `metrics_json` replaces the run's entire `source.latency` block with n=0/p50=0 (`bench_scoring.py:1086-1093` filters `>0`). After any rejudge, the run's headline latency percentiles are destroyed — wrong persisted numbers in the system of record. The DTO twin `_row_to_result_row` (bench.py:2275+) maps both fields, proving the omission is drift, not policy. **Fix:** map `rounds`/`latency_ms` in `_row_to_result`; add a round-trip test.

### M2 · `abort_reason` and `judge_shares_endpoint` never survive the persistence round-trip
`src/vesta/api/bench.py:1227-1272` · write side `bench.py:1080-1090` + `bench_runner.py:761-766`
`mark_aborted` stashes the reason in `config_json`, but `_row_to_run` never lifts it back: `get_run(...).abort_reason` returns `''` after a successful `mark_aborted` **[emp]** — the `BenchRunDetail.abort_reason` field (bench.py:1647, 2354) and the frontend "Aborted: …" panel are permanently empty. `_run_to_row` has no column for the field at all, so the failed-cell path (which sets `abort_reason` on a `status='failed'` row) loses the reason entirely, and `mark_aborted`'s `WHERE status='running'` then skips the already-failed row. Same mapper drops `judge_shares_endpoint` (True round-trips to False **[emp]**), making the detail field misleading. **Fix:** lift both from `config_json` in `_row_to_run`; stash the reason on the failed-cell path.

### M3 · One failed bench cell abandons the matrix to orphaned background tasks
`src/vesta/eval/bench_runner.py:846`
`run_benchmark` awaits `asyncio.gather(*cells)` without `return_exceptions=True` or cancellation. On the first cell exception the gather raises immediately but the remaining cell tasks keep executing detached; `_run_to_completion`'s handler (bench.py:1927-1935) marks every still-'running' cell aborted and pops `_tasks`, so the API reports the group finished while orphaned cells keep burning LLM calls and later overwrite their rows from `aborted` back to `complete` — mutating a "failed" group under the user. **Fix:** `gather(return_exceptions=True)` + cancel outstanding cells on first failure (or `asyncio.TaskGroup`).

### M4 · Deleting an in-flight bench run crashes its cells via FK and triggers the M3 cascade
`src/vesta/api/bench.py:2261-2269`
`delete_bench_run` has no in-flight guard. Deleting a run whose group is executing removes the `bench_runs` row (cascade wipes its question rows); the runner's next `insert_question_result` raises `sqlite3.IntegrityError` (foreign_keys ON **[emp]**), which becomes a cell-level exception and detonates the M3 cascade exactly. **Fix:** 409 when the run's group is in `_tasks`, mirroring the index-trigger guard.

### M5 · Eval run rows: `started_at` empty forever; failed/crashed runs report `done`
`src/vesta/api/eval.py:88-121` (UPDATE never sets `started_at`; placeholder inserts `''`) · `eval.py:276-291` (status) · `eval.py:348-366` (error path)
After a successful run the row reads `started_at=''` with a real `finished_at` **[emp]** — every API-created eval run renders '—' as its date; the frontend already documents this as a workaround (`frontend/src/lib/format.ts:36-38`). Worse, `EvalRunDetail.status` defaults to `"done"` and nothing ever derives `"error"` (the failure is recorded only inside `config.notes`; `EvalRunResponse` documents `running|done|error`), and there is no stale-placeholder reconciliation at startup — a crashed or failed eval run is indistinguishable from a legitimate all-zero retrieval run. **Fix:** include `started_at` in the UPDATE; persist an error status; reconcile placeholders in the startup sweep.

### M6 · The confidence-gate calibration feature is wired to nothing
`src/vesta/retrieval/__init__.py:92-187` (4 settings) · `src/vesta/eval/calibrate.py` (202 LOC) · `src/vesta/cli.py:2121-2122`
The four `retrieval.confidence.*` settings (hot=True, UI-exposed) have **zero readers** — no constant-name consumer and no string-key consumer anywhere in src/tests/scripts (verified both forms). `vesta bench calibrate` fits thresholds, writes them as settings, and the CLI comment claims "the running app picks them up immediately" — but no pipeline, agent-loop, or answer path reads them; the agent loop has no confidence gate at all. The whole knob cluster plus the calibration report is a placebo: tuning it changes nothing. **Fix:** either implement the gate the docstrings describe (calibrate.py:1-13) or delete the settings + calibrate surface; don't ship knobs that pretend to work.

### M7 · llama-server supervisor: `stop()` still races in-flight starts and leaks the child (0822-M3 incomplete)
`src/vesta/inference/local.py:241-255, 441-457` · rebind path `inference/__init__.py:400-405`
Three residual defects in the M3 fix: (a) `stop()` takes no `self._lock` while `_start_and_wait` assigns `self._proc` only *after* `create_subprocess_exec` (local.py:241) — a settings rebind between the exec await and the assignment reaps nothing (`_proc` still None) and the old supervisor is dropped with a live child holding port 8081; every later spawn dies on bind through all 5 retries. (b) `_abort_start` guards with `except Exception` (local.py:253) — a `CancelledError` delivered mid-exec bypasses the cleanup entirely, orphaning a spawned-but-unassigned child. (c) `ensure_running` awaits a live restart task with `suppress(Exception)` (local.py:205-208) while `stop()` cancels it without awaiting — the CancelledError propagates into the chat caller as a dropped connection. All three leave "local inference broken until the stray process is killed", the exact M3 symptom. **Fix:** lock (or `_stopping` flag) in `stop()`; `except BaseException: await self._abort_start(proc); raise`; suppress CancelledError in the restart-wait.

### M8 · Index sidecar/wipe cross-arm hole: API wipe + stale CLI sidecar ⇒ silent permanent holes (0822-M8 incomplete)
`src/vesta/api/zims.py:601-628` (delete_index) · `src/vesta/cli.py:2561-2585` (sidecar) · `src/vesta/index/job.py:214-227` (fresh branch)
The M8 fix unlinks the CLI sidecar only under CLI `--fresh`. The server arm has two wipe paths that never touch `data/.index_progress_<zim>.json`: `DELETE /api/zims/{id}/index` (depth→0, no pending-job guard either — S2) and a server-triggered depth change. After either, the CLI sidecar still says `{done_count: N, depth: d}`; a later plain `vesta index --depth d` matches the stale depth, "resumes" at N into the wiped store, finishes, and marks `complete` — articles 0..N permanently missing from a "complete" index. The server job's own `job.checkpoint({"done_count": 0})` (job.py:227) writes the *jobs-table* store; the two resume stores remain separate. **Fix:** delete_index and the API index-trigger's fresh branch must unlink the sidecar (or the CLI must validate the sidecar against `zims.index_status/index_depth` before trusting it).

### M9 · Chat route persists only the merge delta — Round-0 cards vanish from restored conversations
`src/vesta/api/chat.py:225-227` · delta emission `agent_chat.py:2685-2694` · protocol `docs/sse-protocol.md` "Sources merge"
`_run_chat_turn` does `sources_json = _dumps([event.cards])` on **every** SourcesEvent, replacing the previous value — but a `merge:true` event carries only the delta cards. Any agent turn whose tool rounds discover new cards persists an assistant row whose `sources_json` holds only the late cards: Round-0 (or the follow-up first-discovery) cards are dropped from the system of record. The live SPA appends correctly, but on reload (`session.svelte.ts:33` restores `assistant.sources`) the conversation shows 1–2 cards where 6+ streamed, and the persisted answer's `[n]` markers reference card numbers that no longer exist in the row. The FIX1/FIX2 tests fake only `iter_answer_events`, which never emits merge — uncovered. **Fix:** accumulate cards across events (append on `merge=true`).

### M10 · `run_one_turn` returns the pre-recovery answer when `answer.agent.answer_cleanup=false`
`src/vesta/api/agent_chat.py:2437, 2471-2477` (verified) vs streaming driver `:2652`
`answer` is bound once from the main run and only re-synced inside `if ctx.answer_cleanup:` — so with the registered (hot) `answer.agent.answer_cleanup=false` arm, every recovery mutation of `st.answer` (no-tool fallback `:2216`, compact re-ask `:2271`, abstention retry `:2339`) is discarded: the bench driver returns the raw pre-recovery answer, and on a UsageLimitExceeded/overflow crash it returns an empty string while `trace.compact_reask.fired=True` claims a re-ask answer. The streaming driver rebinds unconditionally (`answer = st.answer`). Bench (`bench.py:467` drives `run_one_turn`) persists wrong `answer_text`/`abstained` on exactly the recovery arms the shared core owns — the two drivers diverge on identical turns. **Fix:** `answer = _cleanup_answer(st.answer) if ctx.answer_cleanup else st.answer`.
### M11 · Force-quit of `vesta index` strands `index_status='running'`; startup then seeds VECTORS from the partial index (0822-L10 escalated)
`src/vesta/cli.py:2589-2598, 2608-2610` · `src/vesta/index/job.py:174-179` · `src/vesta/index/__init__.py:181-182`
The second Ctrl+C raises `KeyboardInterrupt` — a `BaseException` — which bypasses `IndexZimJob.run`'s `except Exception` error-stamping arm (job.py:174-179, verified) after the graceful-pause branch was already skipped; the row keeps the `'running'` stamped at job start ("forced quit. Index state left as-is" prints while the row claims a live run). Same stranding on SIGKILL/power loss, and nothing reconciles it: the startup sweep handles bench rows only, `reconcile_stale` touches 'stale' only. The sharpened impact: `reseed_indexed_state` counts `index_status IN ('complete','running')` as indexed (index/__init__.py:181-182, verified), so the next app boot seeds `Capability.VECTORS` from the stranded row and hybrid/vector_knn profiles silently serve a **partial** vector store while the UI shows the archive running forever. **Fix:** stamp 'paused' on a `BaseException` arm before the finally, and reconcile stranded-'running' rows (no live lease holder, no live job task) in the startup sweep.

### M12 · Degraded CLI boot silently persists poisoned eval/bench rows that poison `--baseline` (0822-L5 confirmed)
`src/vesta/cli.py:1598-1599, 1642-1646` (suppressions, verified) · persistence `cli.py:1773-1794` · guard `eval/runner.py:433-437`
`_open_runtime` swallows registry-start and gateway-build failures with no logging (main.py logs both), then `vesta bench retrieval` evaluates over a dead registry: per-query `NoCandidatesError` is caught, `_reduce_metrics` derives `degraded` only from capability-drop traces so an empty-corpus run reads degraded=False, the report prints "degraded: none", and `_eval_run` persists unconditionally — an all-zero recall row lands in `eval_runs` looking clean and passes `compare()`'s only guard, skewing every later `--baseline` comparison. Bench twin: with `gateway=None` every LLM system returns per-question error *data* ("no LLM gateway configured") and `_run_cell` still stamps the run `complete`. **Fix:** log the suppressions; refuse or stamp degraded when zero archives are enabled or every question errored.

### M13 · Server-side pause/cancel of an index job leaves `zims.index_status='running'` — VECTORS seeded over a partial index via the ordinary pause button
`src/vesta/jobs/runner.py:131-141` (pause, verified) · `src/vesta/index/job.py:276-292, 174-179`
`pause()` sets the flag then unconditionally `task.cancel()`s; the CancelledError is delivered at the task's current await, so `_run_build` unwinds without ever reaching the loop-top cooperative `job.cancelled()` check that stamps the zims row 'paused' (job.py:276-287) — that branch is reachable only from the CLI's flag-only handle and the flag-first test. The job *row* gets 'paused' (runner.py:287-289) but the *zims* row stays 'running' forever (same for cancel), and since `reseed_indexed_state` counts 'running' as indexed (index/__init__.py:181-182), the M11 silent-partial-serving mechanism is reachable with **no crash** — one click on the pause button. **Fix:** catch `asyncio.CancelledError` in `IndexZimJob.run`, stamp zims 'paused' + progress, re-raise; or make pause() flag-only.

### M14 · `JobRunner.stop()` terminal-cancels running jobs: every clean restart forfeits checkpoints
`src/vesta/jobs/runner.py:91-104, 283-292` (verified)
`stop()` cancels every live task; the CancelledError handler branches only on `pause_requested` — a shutdown cancel takes the `_finish(job_id, "cancelled")` arm, which is terminal, and `resume()` rejects terminal rows (runner.py:146). `start()`'s crash-resume (only 'running' rows, :74-88) never fires after a *graceful* restart — tests/test_jobs.py:278-282 pins exactly this by manually re-stamping the row 'running' to test restart survival. Consequences: a multi-GB ZIM download cancelled by restart, once resubmitted, gets a checkpoint-less row and the downloader unlinks the `.part` ("never append onto an unknown file", catalog/download.py:245-248) — full re-download; an index build cancelled by restart re-triggers as fresh (`start_at==0` ⇒ `delete_by_zim` + `_reset_articles`, job.py:215-221) — full rebuild after hours. Contradicts the runner's own contract ("jobs persist… survive a restart by resuming from their last checkpoint", runner.py:3-6). **Fix:** branch on `self._stopping` and stamp 'paused' (or leave 'running') on shutdown cancels.

### M15 · The runner never re-validates the job row when a parked task finally runs — S1's double-run plus two resurrection races
`src/vesta/jobs/runner.py:143-154, 234-259` (verified)
`resume()` accepts 'queued' rows and `_enqueue` spawns unconditionally, overwriting `state.task` without cancelling a live predecessor; `_acquire_and_run` validates only in-memory flags, never the row status. Interleaving A (double-run): job B queued behind a same-type limit-1 job with T1 parked on the semaphore; resume(B) spawns T2; T1 later acquires and completes the job, then T2 acquires and **re-runs the completed job** — for `download_zim` the re-run hits "not part_path.exists()" and re-downloads the entire multi-GB file from zero. Interleaving B: cancel(B) then resume(B) clears `cancel_requested` (:149-151) while parked T1 still delivers its CancelledError → row terminal-'cancelled' — then T2 runs the job the user cancelled. Interleaving C: a queued server index job marked 'cancelled' by the CLI supersede arm still runs when the semaphore frees (no lease held while queued) and surfaces a spurious IndexLeaseHeld error job. **Fix:** reject/supersede resume when a live task exists; re-read the row at acquire time and finish-as-cancelled on terminal status.

### M16 · `headroom_realised` divides system-over-all-questions by ceiling-over-oracle-subset — exceeds 1.0 on the shipped dataset
`src/vesta/eval/bench_scoring.py:839-873` (verified) · surfaced via `BenchRunSummary.headroom` (bench.py:2325)
`reference_points` counts `system` over **all** results but `ceiling` only over oracle-bearing results — and the shipped dataset carries oracle on 50/200 questions, closed_book on 0/200. Reproduced: 6 questions, 3 oracle-correct, system 5 correct → headroom (5−0)/(3−0) = **1.67**; on the real dataset a 120/200 run reports 2.4. Same partial coverage zeroes floor and retrieval_regressions for every run, and the denom==0 arm returns 1.0 regardless of system. The unit test covers only full oracle coverage. Persisted in `metrics_json.reference.headroom_realised` — wrong persisted headline number. **Fix:** compute system/floor/ceiling over the same reference-bearing subset, or suppress when coverage < total.

### M17 · Abstention metrics read the judge's echoed `abstained` field instead of the harness decision — and rejudge computes them differently
`src/vesta/eval/bench_runner.py:695-713` (verified: `score_question(..., outcome=outcome)`; harness `outputs[q.id].abstained` never passed) · parse `bench_scoring.py` (`bool(obj.get("abstained", False))`, forced False on UNJUDGED)
`answer.abstention.*` headline metrics (`over_refusal`, `hallucination_rate`, `abstention_correctness`) are built from the judge's JSON echo, which defaults False on omission or judge failure. Reproduced: harness abstained=True with a judge verdict omitting the field → `over_refusal=0.0` where truth is 1.0 and `abstention_correctness=1.0`; judge endpoint failure on a correctly-abstaining out-of-corpus question → `hallucination_rate=1.0` where truth is 0.0. `_rebuild_scored` (rejudge) uses the row's harness `abstained` instead — identical verdicts shift abstention numbers between a run and its rejudge. **Fix:** thread the harness abstention into `score_question`; make live and rebuild paths agree.

### M18 · The shipped "How thorough" control is a placebo — the composite plus eight `retrieval.*` settings have zero backend readers
`src/vesta/retrieval/__init__.py:57,68,125,136,147,158,171,194,206` · UI `HowThoroughControl.svelte:26-52` + `settings-basic.ts:27-28` (verified)
The Basic-settings "How thorough" pills write `retrieval.stage_b.shortlist` (10/20/40) and `retrieval.context.max_per_article` (2/4/6) with the caption "retrieve N → keep M per article" — and nothing ever reads them: the only references repo-wide (both constant and string forms) are the declarations, `__all__`, comments, and the frontend writes. Same for six more package settings: `retrieval.candidates.max_articles`, `retrieval.stage_b.candidates_max`, `retrieval.rerank.enabled` (hot), `retrieval.context.ordering`, `retrieval.context.budget_tokens`, `retrieval.dense.k`, `retrieval.dense.enabled` (hot). Live behavior is entirely the profile YAML `Params` (standard hardcodes shortlist 40 / per-article 6 / budget 12000) — "Fast" leaves retrieval identical at 40/6. pipeline.py:48-51 admits the context pair are "mirrored defaults… not pipeline-enforced", and even that mirror is void: the dev-console form is generated from static Params defaults. Same class as M6/N21, but sharper — a shipped interactive control that lies. **Fix:** overlay these settings onto resolved profile Params at the composition roots, or delete the settings and the composite.


---

## Medium findings (new)

- **N1 · Malformed or name-form `?scope=` silently searches all archives on `/api/search`.** `src/vesta/api/search.py:104-110` — `with suppress(ValueError)` drops the whole scope to None when any token is non-numeric; `/api/answer` fixed this exact bug class (`_parse_scope`: degrade to matches-nothing, resolve name tokens, warn — answer.py:368-405) but the search twin kept the retired pattern. The documented name-form scope works on one endpoint and silently unscopes the other. Reuse `_parse_scope`.
- **N2 · Job-type param validation still gates only `download_model`.** `src/vesta/api/jobs.py:49-85` — every other registered type accepts arbitrary params; `refresh_catalog` takes `params.url` verbatim into a server-side fetch with redirects followed (see carried A1 — this *widens* the SSRF surface to any job-submission client).
- **N3 · LLM API keys returned cleartext by the settings API.** `src/vesta/api/settings.py:88-93` — `GET /api/settings` returns every resolved setting incl. `inference.llm.api_key` / `eval.judge.api_key`; no redaction anywhere (repo-wide grep for redact/secret/mask: zero hits) and no `secret` marking in the schema, so the UI renders the key in plaintext too. Trusted-LAN mitigates exfiltration, not log/history/copy-paste leakage. (The security pass additionally reports keys persisted into run `config_json` snapshots and served back by run-detail endpoints — not independently re-verified.)
- **N4 · Free-form `golden_set` silently launches the full pinned-archive run.** `src/vesta/api/eval.py:202` + `eval/golden.py:254-258` — any unrecognized name falls back to the full set, so a typo (`"fixture_subsets"`) launches the expensive run instead of a 422 and pins it under the typo'd name. Validate against the known set.
- **N5 · `eval.archive.path` / `eval.archive.checksum` declared but unwired.** `src/vesta/eval/golden.py:129-145` — UI-exposed settings; every consumer (golden.py:223-224, api/eval.py:322/397, cli.py:1785/1953/2281) reads `.default`, so relocating the pinned archive is a silent no-op and CLI verify never finds it. Same class as the removed 0822 A4 `zim.dir` knob.
- **N6 · Streaming driver discards all main-run usage on crash paths; bench driver keeps it.** `agent_chat.py:2614-2620` vs `:2430` — `run_stream` is called without the `usage=` parameter it supports, so a UsageLimitExceeded/overflow exception loses the result binding and the trace reports zero main-run tokens (only recovery usage folds in); `run_one_turn` threads a shared `RunUsage` and retains the full accounting. Persisted conversation traces understate tokens precisely on the overflow paths the counter exists to measure.
- **N7 · Window-plan fit arithmetic ignores `message_history` in every branch that carries it.** `agent_chat.py:1777-1789` — `seed_fit_chars` subtracts prompt/directive/reserve/HEAD-TAIL/question but never `_wire_chars(message_history)`, though the derived tool budget and the abstain-retry fit check both count it; with `contextual_followups=off` a history-bearing turn's first request can 400, the follow-up branch (`:1799-1803`) has no first-request fit check at all, and the overflow fallback (`:2201-2203`) runs with **no** history — a follow-up that overflows recovers with zero conversational context.
- **N8 · A follow-up turn's only SourcesEvent is lost when the stream crashes after a search.** `agent_chat.py:2620-2626, 1364-1374, 2685` — the follow-up first `sources` event is buffered inside `_do_search` and drained only inside the `run_stream` body; if `run_stream` raises after a tool round (e.g. UsageLimitExceeded on a later model request), the buffer dies with it and the trailing merge delta excludes exactly the latched `first_sources_keys` — no sources event ever reaches the client, and the fallback answer cites cards it never received.
- **N9 · `budget_exhausted` error followed by trace and done despite error being terminal.** `agent_chat.py:2698-2705, 2739-2740` (sibling shape at `:2585-2590`) — the frozen protocol says error terminates the stream; the generator keeps emitting trace+done after it (adjacent to, distinct from, the recorded-open C1 serializer in chat.py).
- **N10 · Agent search discards the composition root's model-facing text — term-surfacing is dead while its retrieval still fires.** `agent_chat.py:1344-1346` (verified) vs `answer.py:931-941` — `_do_search` returns `result.text` only when the passage pool is empty; otherwise it re-renders from passages. The iter-10 term-surfacing ladder (up to 5 extra full `run_pipeline` calls on weak 4+-word searches) appends surfaced titles only to `result.text` → never reaches the model: pure wasted latency, and the measured recall mechanism doesn't exist at runtime. The Round-0 reformulation visibility block (`answer.py:899-913`) is discarded the same way. Fix: append `result.text`'s candidate blocks in the non-empty branch (or stop firing `_surface_term_candidates` on the agent path).
- **N11 · `read_article`'s 32k stage-1 window can drop the retrieval-best snippet the stage-2 guarantee promises.** `answer.py:987-997` → `agent_chat.py:1619-1621, 1218-1247` — stage 1 runs `focused_view(..., 32_000)` with no `must_include_spans`; stage 2's snippet-forcing `text.find(snippet)` then runs on the already-elided excerpt — for articles >32k chars the snippet may be gone (`find()==-1`, spans=()), so the model never sees the passage retrieval scored highest. Focus.py's own guarantee ("strict superset of what retrieval found") holds only ≤32k. Fix: thread the card snippet into stage 1.
- **N12 · `chat.history.strategy='summarize'` is a placebo choice.** `answer/__init__.py:59-69` (UI help advertises "compress older turns into a summary") vs `conversation.py:50` — `build_history` never reads the strategy; "summarize currently == truncate" per its own comment. The code admits it; the settings UI does not. Drop the choice until a summarizer exists.
- **N13 · Citations contract drift: frozen protocol doc documents retired machinery and a retired setting the frontend still reads.** `docs/sse-protocol.md:71-84` cites `renumber_inline_citations` (exists nowhere in src) and `answer.citations.min_span_score` (retired by migration 0012; backend now emits constant `score=1.0` at `citations.py:60`) — while `AskTurn.svelte:43-44` still resolves the retired key into `minSpanScore`, so the "weakly supported" chip treatment is unreachable twice over. Update the doc, remove the phantom read.
- **N14 · Aborting a stream via the Use-AI toggle leaves the turn spinning forever.** `answerStream.svelte.ts:81-85` — `stop()` exits `consume()` without marking the state; `SearchPage.setMode` aborts mid-stream but deliberately keeps the session. After AI→sources→AI the restored turn shows spinner + "Generating…" + ever-ticking elapsed counter, and the follow-up input is permanently disabled until Regenerate/New-question. Mark the state terminal in `stop()`.
- **N15 · Deleting the currently-open conversation strands every follow-up on a bare 404.** `AskHistory.svelte:28-35` filters only the popover list; the live session keeps the deleted `conversationId`, `POST /api/chat` 404s for it (chat.py:118-128), and `sse.ts:97-100` reduces non-2xx to a synthetic "HTTP 404" that discards the body's reason. Reset the session + URL when the open conversation is deleted.
- **N16 · Every welcome "Ask a test question" click persists a real conversation.** `welcome/+page.svelte:172-180` drives `streamChat` with no `conversation_id`; the backend unconditionally creates and persists the exchange — each repeatable click permanently seeds another demo conversation into history. Thread a no-persist flag or clean up on teardown.
- **N17 · Three unsynchronized `rescan()` triggers race; the loser aborts the whole scan and leaks an archive handle.** `registry.py:724-780` (no lock) — triggers: startup `start()` (:537), `POST /api/zims/scan` (zims.py:274-279), and the download-complete callback (main.py:275-276 ← download.py:282-287). Two overlapping scans both read `zims` rows *before* either INSERT (:743-745), both classify a newly-dropped archive as new, and the second `INSERT INTO zims` hits `UNIQUE(uuid)` → `IntegrityError` propagates out of the loop: the scan endpoint 500s, the entire remaining scan aborts (alphabetically-later archives unprocessed), and the loser's already-opened `LibzimArchive` handle is never stored or closed. Serialize `rescan()` with an `asyncio.Lock`.
- **N18 · Alias mining lacks the per-entry tolerance its siblings promise — one corrupt entry bricks that archive's registration and aborts the whole rescan.** `aliases.py:44-54` — only `get_redirect_entry()` is guarded; `_get_entry_by_id(i)` and `entry.title`/`is_redirect` are unwrapped, though the probe path (:735-739) and media manifest (:825-828, "never let a bad sidecar block registration") both fail per-entry. A single corrupt redirect entry in a 400k-entry ZIM raises out of `mine_aliases` *after* the `zims` row INSERT committed (`_register_new` :785-818) → archive left half-registered (row present, no aliases/media/documents manifests), every subsequent rescan dies the same way, and the rescan loop aborts for later files. Wrap the per-entry accesses like the siblings.

- **N19 · Pausing archive #1 in a queued `vesta index` run immediately launches archive #2.** `cli.py:2625-2628` returns 0 on pause; `_run_index_many`'s loop breaks only on `rc != 0` (`:2483-2486`) — one Ctrl+C to stop the session starts full indexing of the next archive against intent, two more presses per remaining archive. Propagate a distinct pause exit and break the queue on it.
- **N20 · `vesta models` and `bench hardware` resolve settings from default+env only (L7 confirmed).** `cli.py:2212, 2253-2255` — neither loads the settings table, so DB-customized `encoders.*` repos are ignored (wrong repos fetched; the running app then can't find its configured models) and `bench hardware` measures default-repo encoders; `_cmd_bench`'s pinned-archive default is the CWD-relative literal `Path("data/zims")/EVAL_ARCHIVE_PATH.default` (`:2277-2280`) — ignoring a customized `data.dir` and breaking from another directory.
- **N21 · Seven more placebo settings with zero readers (beyond the M6 confidence cluster).** `config/__init__.py` — `server.auth.password` (:59-66, no auth middleware exists; frontend documents "not enforced", yet `SERVER_HOST` help still tells users to pair 0.0.0.0 with this password — a placebo *security* control), `zim.extract_processes` (:135-145, callers hardcode 4 while extract.py:379's docstring claims callers size it from this setting), `query.stopwords.enabled` (:146-154, the real gate is the profile param), `query.ladder.enabled` (:168-176, ladder unconditional), `passages.target_tokens`/`passages.sentence_aligned`/`passages.breadcrumb.enabled` (:177-204, all hardcoded at the only split_passages call sites). All UI-exposed, several hot=True. Related: `query.stopwords.list`'s help misdescribes scope — preprocessing never reads it; its sole reader feeds read_article budgeting (`answer.py:354-366`).
- **N22 · `cancel()` on a paused job reports success but the row stays 'paused'.** `runner.py:156-169` — the paused row isn't terminal so cancel takes the live-task branch, but `state.task` is the *finished* pause task: `Task.cancel()` on a done task is a no-op, the else-branch `_finish('cancelled')` never runs, `api/jobs.py:127-130` replies `{"status":"cancelled"}` regardless, the per-job SSE stream waits forever, and a later resume clears the cancel intent. Call `_finish` directly when the row is 'paused' or the task is done.
- **N23 · Jobs stranded 'queued' at crash time are never restarted.** `runner.py:74-88` — start() re-queues only 'running' rows; a job parked on its type semaphore at SIGKILL/power-loss sits 'queued' forever with no task in the new process (clean stop doesn't strand this way — parked tasks get terminal-canceled). Re-queue 'queued' rows alongside 'running'.
- **N24 · `zims.index_progress` is zeroed by every status stamp, and the per-tick mirror has no production reader.** `job.py:429-431` (`_set_index_status` always writes `index_progress=0` — the 'complete' stamp resets progress right after the last tick mirrored done==total) + `_update_index_progress` (:454-458) is an extra UPDATE on the serialized write connection per tick across multi-hour builds; zero frontend consumers (the only endpoint returning it is already in the dead table). Stop zeroing; move the mirror into the throttled `job.progress` path or drop it.
- **N25 · Vector upsert can silently drop whole batches if the dim-table DDL is rejected.** *(n.v.)* `sqlite_vec_store.py:112-146` — `_ensure_dim_table` failure only logs; `upsert` `continue`s to the next dim group, the build still stamps 'complete' and records index_meta: a 'complete' index with zero vectors that dense queries happily run against. Environment-dependent (needs a sqlite-vec build rejecting both DDL variants), but it violates the module's refuse-don't-fabricate posture — raise instead.
- **N26 · `docs/release-test-plan.md` is stale against HEAD and unusable as written.** Port chaos (doc says host 5329 at :37-38 and uses 5239 in every command; compose maps `0.0.0.0:5129:8080` at docker-compose.yml:16-18, verified); the pre-flight expects a `./data2` bind that compose doesn't create and warns *against* touching the `./data` compose actually binds (:40,74-83 vs compose.yml:20); the mission model `LFM2.5 1.2B` preset doesn't exist (models.py ships only `qwen3.5-4b-q4_k_s`); two phases poll `index_status: "ready"` — not in the enum (zims.py:95: none|running|paused|complete|stale|error); "~110 settings" vs 127 declared. Regenerate or mark superseded.
- **N27 · Two tests actively pin recorded bugs.** `tests/test_chat.py:574-575` asserts `done` follows the terminal `error` event ("Terminal success marker…") — defending the recorded-open C1 against the frozen protocol's "error terminates the stream"; `tests/test_chat.py:49-53` asserts `strategy="summarize"` behaves identically to `truncate` — enshrining placebo N12. Both verified. Fixing C1/N12 will fail these tests; invert/replace them with the remediation.
- **N28 · `bench_authoring/validate.py`'s schema net misses the loader's required per-source fields and diverges from the author script on redirects.** `validate.py:25-49` checks question-level keys only, never `sources[i].{zim,article_title,article_path}` — all three hard-required by `_parse_source` (bench_dataset.py:265-272, ValueError); and its extract helper returns '' for redirect entries while `author_scenario_50.py:26-30` follows `redirect_target` — a redirect-path source the authoring script accepts is false-failed by the validator.
- **N29 · Two ruff versions gate the tree.** `.pre-commit-config.yaml:10` pins ruff-pre-commit v0.9.9 while pyproject/CI run 0.16.0 — verified empirically tolerant today (0.9.9 silently ignores the `UP046/UP047` ignores; CI #7 green at 56aff0b), but local `pre-commit run` under-enforces vs CI and the 0.9.9 format hook can auto-rewrite in a style 0.16 rejects once formatter defaults diverge. Bump the rev or drop the ruff hooks from pre-commit.
- **N30 · More sse-protocol.md drift beyond N13.** `:200` documents `answer_reset.reason` as `fallback|abstention_retry|""` but the emitter also yields `compact_reask` (agent_chat.py:2255,2304) and `cleanup` (:2659); `:5` cites `tests/test_sse_protocol*.py` which doesn't exist (it's tests/test_answer_sse.py + fixtures/sse/); `:259` lists error code `stream_error` no production emitter constructs. Extend the enum, repoint the citation, drop the dead code.
- **N31 · `bake_reference.py` rewrites the golden dataset at `indent=1` while the committed file and sibling author script use `indent=2`.** `bake_reference.py:96` — first `--write` reformats all 200 questions: whole-file diff, blame destroyed, merge conflicts with concurrent GT edits. Content hash is parse-based so nothing semantic breaks — pure repo-hygiene trap.
- **N32 · Dockerfile runtime stage runs as root; first run leaves root-owned files in the host bind mount.** `Dockerfile:100-116` (no USER) — zims/, models/, vesta.db, cache/ all become root-owned, breaking the README's "backing up or moving Vesta is a file copy" without sudo. Possibly deliberate appliance posture (verify owner intent); `USER app` + entrypoint chown is cheap.
- **N33 · Judge cache key omits temperature, max_tokens, and endpoint identity.** `bench_scoring.py:508-522` — `bench_judge_cache` is a global persistent table keyed on rubric‖qid‖answer‖model; verdicts minted at temperature 1.5 / endpoint A are served byte-identical after re-tuning to 0.0 or re-pointing the endpoint — a "re-judge" measures nothing new. Fold the sampling params + endpoint into the key.
- **N34 · `dataset_hash`/`subset_hash` omit measurement-affecting fields.** `bench_dataset.py:143-170` — the hash omits `answer_detail` (embedded in every judge prompt), `sources[].required` (flips SourceMetrics denominators and the attribution 2×2), `capability`, `difficulty`, `level`, `status`. Two datasets differing only in `required:false` hash identically yet score differently — the B7 comparability pin is weaker than it looks.
- **N35 · Calibration measurement ignores `bench.calibration_path`, is CWD-relative, and hardcodes `expected_behavior='answer'`.** `bench_scoring.py:663-713` + `bench_runner.py:822-825` — the configured setting is never resolved (same class as recorded B3, not in its list); the CWD-relative default silently loses calibration outside the repo root (every run trusted=False); and every calibration item is judged under the answer rubric, making the documented abstention-direction items impossible — the shipped file indeed contains none. Also: retrieval_only groups are stamped trusted=False though they never judge.
- **N36 · Bench `metrics_json` carries no degraded signal — runner.py/bench_runner.py metric parity drift.** `bench_runner.py:563-660` never reduces trace degradations, though the eval twin does (`runner.py:296-313`) and `compare()` refuses degraded-vs-clean. A bench cell whose profile silently capability-dropped VECTORS produces clean-looking rows and `compare_runs` checks nothing — the exact failure mode the eval guard exists for.
- **N37 · `compare_runs` semantics looser than the metrics it sits next to.** `bench_runner.py:1004-1042` — `source_recall` delta includes abstention questions in the denominator (compressed vs `source.recall_at_10`), and fixed/broken buckets treat UNJUDGED/PENDING as wrong with no unjudged marker (judge failures read as regressions).
- **N38 · Measurement edge cases: sub_facts silently dropped; out_of_corpus all-zero row; unbounded rubric payload; empty-model headroom bypass.** `bench_dataset.py:312-320` `continue`s malformed sub-facts (every other field raises) — a typo'd sub-fact silently makes a compositional question plain; `runner.py:292-303` persists an all-zero recall row for out_of_corpus in every run; `_render_rubric` (`bench_scoring.py:397-476`) embeds model_answer unbounded — a pathological answer inflates the judge prompt to 4xx → UNJUDGED with an opaque reason; `bench_scoring.py:874-877` skips the oracle model-match suppression when `answer_model` is empty, yielding bogus headroom on retrieval_only runs.
- **N39 · The normalize preparer forwards inert boolean words as mandatory FTS terms.** `normalize.py:71-80` vs `zim/query.py:227` — the ladder's `_INERT_TOKENS` (not/near/xor/±*/quotes) exists so "booleans don't become literal AND-ed terms", but the primary-path extractor never applies it: DEFAULT_STOPWORDS lacks `not`/`near`/`xor`, so "hotels near the grand canyon" ANDs the literal `near` into every hit; the ladder strips it only on a *zero-result* fallback — partial degradation is silent. Punctuation tokens (`c++`) also pass unsplit on the primary path only.
- **N40 · `section_window` double-counts already-seen neighbours' tokens against the budget.** `section_window.py:86-100` — `group_tokens` sums the whole window group but the append loop skips members already claimed by an earlier winner; `tokens_used` charges the full sum regardless, so overlapping windows reject later winners whose marginal cost fits and the run assembles strictly less context than the budget allows. The "naive reference" test is a copy of the same arithmetic, so it pins rather than catches.
- **N41 · RRF tie order is nondeterministic — fused order inherits concurrent source-completion order.** `pipeline.py:260-271` gathers candidates at coroutine completion (dict insertion order is timing-dependent) and `rrf.py:53-64,124-126` breaks exact score ties by that order; ties are common by construction (`rank_offset=4` deliberately collides entity rank 4 with xapian rank 4), and `candidate_articles` truncates at 40 while per-archive funnels reach 100+ — which tied article survives differs run-to-run on multi-source profiles. Iterate groups canonically (zim_id, source) or add `(path, zim_id)` as final sort key.
- **N42 · RRF global mode fuses by bare path across archives — identity corruption.** `rrf.py:53-59,100-110` — across_archives='rrf' keys candidates by `c.path` alone; two archives sharing a path merge into one entry attributed to the first-seen `zim_id`, the other archive's nomination is discarded, and downstream extraction under the wrong archive silently drops the candidate. No shipped profile uses the mode, but it's a registered, documented benchmark control. Key by `(zim_id, path)`.
- **N43 · Profile YAML validation silently drops unknown keys and params; `rrf.k:0` crashes into the unflagged fallback.** `profiles.py:75-96` — a typo'd top-level key (`scorer:`) is silently ignored (a hand-saved profile can run with zero scorers, no error, against the module's own "validation error, not silent default" posture at :106-109); component `Params` are pydantic with default `extra='ignore'` (`limt:3` silently runs limit=5); and `rrf.Params.k` has no lower bound — `k:0` → ZeroDivisionError per rank-0 candidate → swallowed by the pipeline's fuser except into the *unflagged* `_flat_union` fallback (the recorded R3 gap). Reject unknown keys, `extra='forbid'`, `k` ge=1.
- **N44 · Negative-score regime inverts `lead_boost` and `diverse`.** `lead_boost.py:130-140` (negative × (1+boost) gets *more* negative — boost becomes penalty) and `diverse.py:82,100` (relevance = score/max inverts when max is negative — selects worst first). Reachable via the documented `cross_encoder.enabled:false` A/B toggle or CROSS_ENCODER-unmet chains ending at static_pass's raw cosines; shipped profiles avoid it only because the cross-encoder sigmoid rescales to (0,1). Clamp/shift at the assembler boundary. Related degenerate: an all-stopword query ("how to") gets no title rung while a token-free query does (`query.py:296-322`).
- **N45 · Two P3 residues from the remediation sweep.** `registry.py:70-77,551-568` — the registration executor's "hard-capped at 2" comment is per-pool, not global: N concurrent registrations spawn N mining threads (amplified by N17's racy rescan triggers); no leak, threads exit after ~29 s. `cli.py:1270-1274` vs `:767-775` — the judge-clamp endpoint sourcing diverged between the bench-run arm (passes `""` unless `--judge-endpoint` is given, so the shared-endpoint clamp can never fire from settings) and f0ade27's verify arm (passes the configured endpoint) — no measurable impact today (judging is stage-sequential), but the twins diverged at exactly the seam the 6f899f7 consolidation was meant to freeze. Also: `bench verify --concurrency/--judge-concurrency` help claims "default: bench.max_concurrent" but resolves `.default` (`cli.py:1269-1273`) — folded into B3's row.

## 0822 Medium-tier items — verified status at HEAD

Fixed this cycle (verified): C3 (newest-window history, chat.py:157-159) · C6 (removed) · C7/C8 (P1 slim query + 0015 index) · R5 (P2) · Z4 (P5) · S4 (P3) · S6 (b66592f) · A3 · A4 · A5 (teardown verified clean) · B4 (prune wired at startup) · L1 (effective endpoint feeds the clamp, cli.py:769-774) · L3 (removed) · L9 (P4) · F6 (P6) · F8 (DEV-gated) · I7 (bi-encoder reconfig correctly serialized).

**Still open** (verified at HEAD this cycle):

| 0822 ID | Current evidence |
|---|---|
| C1 · error+done after terminal event | `chat.py:363-368` serializer appends `DoneEvent`; persistence (`chat.py:234`) runs after `done` forwarded |
| C2 · partial answer lost (partial fix) | user turn now persists pre-stream (`chat.py:171`); non-LLM exceptions still skip the assistant row; docstring overclaims |
| C4 · conversations >500 404 | `chat.py:267-270` still 500-scan + linear search |
| C5 · answer_reset before replacement (partial fix) | only the abstain_p6 arm got outcome-first ordering (agent_chat.py:2304-2306); fallback `:2200`, compact-reask `:2255`, abstention-retry `:2324` still reset first with failure arms producing no replacement token |
| I1 · GGUF resume no truncate/checksum | `inference/download.py:117,200` — M5's `os.truncate` went to the catalog twin only |
| I2 · idle-unload failure latches `error` | `runtime.py:481-485`, `_error` cleared only by rebuild |
| I3 · `source` flip never rebinds gateway | `inference/__init__.py:351-388` + `runtime.py:325-356`; source/endpoint absent from restart keys |
| I4 · envelope usage parsed as zeros | `gateway.py:159-167` getter still getattr-only |
| I5 · idle-unload can fire mid-generation | `runtime.py:476` — only load-in-flight guarded |
| I6 · `sleeping` unreachable, status stale | `runtime.py:444-470` never probes router |
| R2 · rewrite bypasses normalize | `conversational_rewrite.py:110-121` manual split, stale `is_keyword_query` |
| R3 · degradation unflagged (partial fix) | `pipeline.py:310-314` records a stage error but no `tr.degraded` → comparisons still unflagged |
| R4 · bare-path article identity (partial fix) | cards now `(zim_id,path)` (`_shared.py:126-134`) but `max_per_article`/`min_articles` still bare path (`:73-83`) |
| R1 · curly-apostrophe escapes (partial fix) | `_CAP_TOKEN` + curly-quote alternatives fixed, but `_QUOTED_RE`'s straight-quote boundary guards still carry doubled `\\u2019` escapes (title_entity_suggest.py:93-99) — U+2019 absent from the class, redundant u/2/0/1/9/backslash present |
| Z1 · no-space text collapses (partial fix) | `_HARD_SPLIT_MULT` cap added, but `_nearest_space_after` still ASCII-space-only with `len(text)` fallback (passages.py:196-205) |
| Z2 · UTF-8-only extraction | `extract.py:176,217,239,249` all `decode("utf-8","replace")` |
| Z3 · mislabeled hot rerank setting | `encoders/__init__.py:135-144` baked into cached cross-encoder |
| S1 · resume double-enqueues job rows | never remediated; concretized as **M15** with three exact interleavings (double-run re-download, cancelled-job resurrection, CLI-supersede spurious error) |
| S2 · delete_index lacks pending-job guard | `zims.py:601-628` — also feeds M8 |
| S3 · capability reseed gaps (partial fix) | reseeded at startup/CLI/delete_index only; archive delete/disable (`zims.py:295-296`) and job error arm (`job.py:176-179`) miss it |
| S5 · ETA inflated by midpoint | `estimate.py:220` still `(low+high)/2`, contradicting the module's own contract |
| A1 · SSRF, now widened | 6 fetch sites, `follow_redirects=True`, zero scheme/host checks; additionally reachable via `POST /api/jobs` `refresh_catalog` (N2) |
| A2 · invalid 206 from Range handling | `zim.py:112-146` never remediated — inverted Content-Range on `bytes=500-400`/past-EOF |
| B1 · gateway swap race | `bench.py:227-230,258` app-global swap, no in-flight guard; overlapping groups nest dead recorders and mis-attribute tokens |
| B2 · abort_reason persistence (read side) | see M2 |
| B3 · settings read `.default` | six remain: `bench.max_concurrent`, `bench.judge.concurrency`, `bench.repeats` (API path) + `bench.judge.cache`, `bench.judge.retries`, `bench.calibration_min_correlation` (runner) — plus the verify-path flag defaults (`cli.py:1269-1273`, new in f0ade27, help claims "default: bench.max_concurrent") |
| B5 · unknown profile → lexical | `api/eval.py:294-307` fallback makes the 404 unreachable; same in `_profile_hash` (bench.py:1759-1764) and cli.py:1672-1678 |
| B6 · attribution drill-down ≠ matrix | `bench.py:2102-2107` still lacks the out-of-corpus exclusion |
| B7 · compare hash guards absent | `bench.py:2168-2207` + `bench_runner.py:987-1047` validate existence only |
| L2 · `--save-context` guard missing return | `cli.py:750-751` warns, runs the full matrix, exits 0 |
| L4 · `--import-old` not idempotent | `bench.py:1371-1445` blind `insert_run` per source row; the "(idempotent)" comment lies |
| L6 · CLI/API twins (partial fix) | pipeline/metric/matrix merged; the profile-fallback resolver is still x4 hand-rolled copies (see Duplication) |
| L8 · FAIL prints exit 0 (partial fix, sharper) | the helper's `return 1` never reaches the process exit: `_eval_run` discards `_print_baseline_delta`'s 1 (`cli.py:1790-1794`) and `_cmd_bench_run` discards compare's; `bench verify` floors/ceilings still `return 0` (`cli.py:1321-1338`) — `--baseline 999 && ship` sees success on a comparison that never ran |
| F1 · SSE silent-EOF (partial fix) | `sse.ts` now emits `transportErrorEvent` on fetch/non-2xx/stream errors, but a stream that *ends* without `done`/`error` still yields nothing and the residual buffer is discarded — the docstring's core promise is unimplemented |
| F3 · stale-response races | no abort/sequence guards in SearchPage/ArchiveBrowsePage/Reader (repo grep) |
| F5 · history popover error latch | `AskHistory.svelte:18-26` never clears `error` at fetch start |
| F2 · failed turn shows success pill (partial fix) | 0822 remediation removed the unreachable error branch but not the cause: reducer still sets `done` on error (reducer.ts:91) and `AskTurn.svelte:127-133` renders the unconditional pill — "Answered · read 0 sources across 0 archives" above the error block |
| F4 · reset race (partial fix) | the landed fix aborts only the SSE controller (session.svelte.ts:79-82); `loadHistory`'s non-abortable `conversationsApi.get` still assigns `priorTurns` unconditionally after `reset()` — stale resolution merges two conversations into one thread |
| F7 · citation-focus effect over-subscribed | sharper: the effect reads `answerState.sources[id]` (AskTurn.svelte:58) and the reducer replaces state on *every* event, so after one chip click during a live stream each subsequent token re-opens the Reader drawer and re-runs its refetch/PDF/poll effects |


---

## Dead code (new)

| Item | Location | Note |
|---|---|---|
| Confidence-gate knob cluster | `retrieval/__init__.py:92-187` + `eval/calibrate.py` | The M6 feature — settings + 202-LOC module + CLI wiring with zero runtime consumers |
| `recommend_preset` | `inference/models.py:108-112` | Zero callers repo-wide; also ignores its `ram_total_bytes` arg while the module docstring claims RAM-based picking |
| `ModelSpec.license` | `encoders/registry.py:48` | No reader; only a test fixture sets it |
| `POST /api/zims/{id}/index/estimate` | `api/zims.py:496-517` | Tests only — catalog cards embed server-computed estimates inline (b66592f); delete or declare script surface |
| `DELETE /api/zims/{id}/index` | `api/zims.py:601-628` | No frontend consumer (no `deleteIndex` anywhere); the SPA can deepen but never lower an index — also M8's wipe path |
| `GET /api/zims/{id}/index` | `api/zims.py:631-651` | Tests only; SPA reads index state from the list payload |
| `GET /api/catalog/{entry_id}` | `api/library.py:255-263` | Tests only; the SPA resolves entries from the list and passes entry_id in the download body |
| `GET /api/jobs/{id}` | `api/jobs.py:88-94` | Tests only; the UI consumes the SSE burst (list endpoint stays — documented) |
| `LlmStatusOut.hardware` | `api/models.py:84` | Serialized, never read (UI consumes `/api/system/hardware` instead) |
| `LivePipelineRunner.__init__(profile)` + system `_endpoint`/`_api_key` attrs | `api/eval.py:166-169`; `api/bench.py:639-641,722-724,817-819` | Accepted/stored, never read — the per-run endpoint/api_key args are inert pins for three of four systems |
| `TurnResult.all_messages` + `_TurnRecoveryState.final_messages` plumbing | `agent_chat.py:2481` (+2165, 2217, 2273, 2341) | No production consumer — chat route persists (role, content) via conversation_store, bench never reads it; docstring's "needed by the chat route" justification is false; sole reference is a test repr canary |
| `format_search_result` full rendering | `answer/tools.py:113-143` | Only its no-passages string ever reaches the model (N10); `[i+1]` numbering already drifted from the card-number policy — a trap for anyone wiring `result.text` through verbatim |
| AnswerContext.query/is_search_term/history + every AnswerDeps field | `answer/contracts.py:186-229`; built per request at `answer.py:198-225` | Write-only DI surface: only sources_only is registered and reads `ctx.retrieval` alone — every GET /api/answer pays a live zims-table SELECT + tool-runtime/reformulator build to feed nothing |
| `answer.py` third `_looks_like_question` copy | `answer.py:306-311` | Survived the 0822 classifier merge by becoming dead; feeds only the write-only `is_search_term` |
| `SearchToolResult.confidence` | `answer/tools.py:71-81` | Populated every search (`answer.py:951`), zero readers — the advertised recovery re-check doesn't exist |
| `answer.tokens.estimate_tokens` | `answer/tokens.py:40-50` | Zero src callers (all production uses `estimate_tokens_for_chars`); tests + docstrings only |
| Two-leg conservative-detector docstring | `agent_chat.py:2380-2385` | Describes machinery that exists nowhere in the repo (repo-wide grep) — delete the paragraph |
| `benchApi.results` + `AttributionFilter` | `lib/api/bench.ts:16,34-44` | Zero callers — detail views fetch via raw `api.get` and filter client-side; server-side verdict/capability/attribution params unreachable from the UI |
| `benchApi.remove` | `lib/api/bench.ts:46` | Zero callers — `DELETE /api/bench/runs/{id}` unreachable from the SPA |
| `JobStreamEvent` + `JobProgressDelta` | `lib/types.ts:149-154` | Zero consumers — jobs store parses raw event/data strings |
| `IndexEstimate` | `lib/types.ts:260-271` | Zero consumers — frontend twin of the test-only estimate endpoint |
| Welcome `ramBytes` fetch + `defaultPreset` | `welcome/+page.svelte:74,269-289` | Fetched/derived, never read — the RAM-tuned recommendation never landed; frontend mirror of dead `recommend_preset` |
| `searchModeEqual` / `contentWords` exports | `lib/search-mode.ts:51-61`, `lib/highlight.ts:15-20` | Test-only / internal-only exports |
| `ChunkSpec.is_lead` | `index/depth.py:60-66,120-177` | Computed at every construction site, never persisted (no column), never read — docstring admits it's speculative |
| `VECTORS_PER_ARTICLE` mapping | `index/estimate.py:74-76` | Zero src callers — both consumers use the band table; docstring's justification ("callers… predate the band") no longer holds |
| `articles.title/char_len/n_sections/flags` columns | `index/job.py:592-604` | Write-only per article per re-index (plus a per-row SELECT) — no query path reads any of them |
| `_write_progress(..., final=...)` | `jobs/runner.py:312-325`, `handle.py:63-67` | Accepted and threaded from two call sites, never referenced in the body — pre-P3 leftover |
| Nonportable one-off analysis scripts | `scripts/bench/matrix_analysis.py:6-8` (hardcoded author-local RUN_IDS), `scripts/round0_probe/*` (machine-bound `/home/loki/...` + session-UUID scratch paths), four scripts hardcoding the author's LAN LLM endpoint (`run_level3_baseline.sh:4`, `phase21_1a_replays.sh:13`, `phase21_5_density_probe.py:36`, `ask_closedbook.py:28`) | Recorded one-off evidence, none re-runnable outside the original box — archive under benchmarks/reports provenance or parameterize |
| `bench.slice` setting | `eval/__init__.py` (declared) | Zero readers — same class as N21's placebos |
| `rubric_prompt_hash` / `RUBRIC_PROMPT_VERSION` | `eval/bench_scoring.py` | The documented run pin doesn't exist — nothing persists or compares it |
| Write-only scored fields | `judge_model`, `citation_supported`, `sub_facts[].source_index` on ScoredQuestion/dataset rows | Populated, never read |
| Judge token category | `bench` token aggregation | Judge-side tokens are never measured or persisted — only answer-side |
| "How thorough" composite + 8 retrieval.* settings | M18 | The shipped Basic-settings control and the package's shortlist/dense/rerank knobs — declarations, comments, and frontend writes only |

(`ENCODERS_INDEX_INTRA_OP_THREADS` is missing from `encoders/__init__ __all__` while every sibling is exported — minor, likely an oversight rather than dead.)

## Duplicated code (drift-prone)

| Cluster | Locations | Already diverged? |
|---|---|---|
| Profile-fallback resolver | `api/answer.py:708-729` · `api/search.py:200-216` · `api/eval.py:294-307` · `cli.py:1672-1678` · hash twin `bench.py:1759-1764` | **Yes** — eval's copy makes its 404 dead code (B5); bench's pins the wrong hash; search's documents its fallback as deliberate |
| Bench result-row mappers | `_row_to_result` (bench.py:1300) vs `_row_to_result_row` (bench.py:2275) | **Yes** — the DTO maps rounds/latency_ms, the store copy drops both (root cause of M1) |
| Scope parsing | `search.py:104-110` vs `answer.py:368-405` | **Yes** — N1 |
| Preset→`ModelPresetOut` mapping | `models.py:176-189` vs `:265-277` | Identical today; two edit sites |
| Settings timestamp | `settings.py:27-29` `utc_now_iso` vs inline copy `setup.py:36` | Identical today |
| Media/document wire shapes | `zims.py:219-239` DTOs vs `search.py:245-316` hand-built dicts | Same keys by hand — a DTO field added on one side won't reach the other |
| `_msg` download progress formatter | `inference/download.py:210-214` vs `catalog/download.py:552-556` | **Yes** — same name/signature, MB-style vs bytes-style output |
| LLM builders bypassing the bound runtime | `answer.py:736-807` (`_build_rewriter`/`_build_reformulator`) vs `_resolve_llm` (`agent_chat.py:572-637`) | **Yes** — twins read `inference.llm.model` from raw settings (with a hardcoded fallback model), not the router-resolved runtime target; a remote rebind leaves rewrite/reformulate pinned to the stale value |
| Agent-turn trace assemblies | streaming `agent_chat.py:2665-2676, 2703-2738` vs `run_one_turn` `:2483-2518` | **Yes** — `agent_llm` timing stage exists only in the user-facing trace, and streaming double-counts recovery wall time into it; per-stage breakdowns incomparable between the driver bench measures and the traces users read |
| Welcome card helpers | `humanizeName`+`KNOWN`+`FLAVOURS` ×2 (`FeaturedCard.svelte:35-48` vs `ArchiveCheckbox.svelte:26-40`); `presetHost` ×2 + `presetDownloading` ×2 (`welcome/+page.svelte:368-386` vs `AiSection.svelte:235-237,366-371`); `curatedToEntry`/`scanForFiles` ×2 (welcome vs catalog pages) | **Yes** — `presetDownloading` implementations differ (jobs scan vs activeDownloadJob check); "medicines" handled differently in the two humanizeName copies |
| `_calibrate` settings write-through | `cli.py:2117-2133` vs shared `persist_settings_and_reload` (`api/settings.py:28-42`) | Hand-rolled ritual + stale "Caller holds the write transaction" comment describing a contract neither has — not diverged today; route through the helper |
| `_now_iso` ×4 in the jobs/index slice | `runner.py:465`, `leases.py:41`, `job.py:431` inline, `cli.py:2700` inline | Identical today; feeds staleness math that must stay ISO-parseable |
| IN-list placeholder builders ×3 | `sqlite_vec_store.py:237,322`, `job.py:636` | Tandem-edit requirement |
| ZIM-extract helper ×3 (diverged on redirects) + same ZIM path constant ×3 | `author_scenario_50.py:26-30` (follows redirect) vs `validate.py:46-49` (returns '') vs `zim_read.py:30-36` ('' + target) | **Yes** — the N28 false-fail is this cluster's divergence |
| phase21_* analysis plumbing | per-run row fetch ×4, `SCORED` tuple ×3, same-named `analyze_run` with different semantics (1b vs 2; 5 imports 2's) | **Yes** — deleting/renaming phase21_2 breaks phase21_5 |
| Profile-hash computation twins | `retrieval/profiles.py` hash vs `bench.py:1759-1764` `_profile_hash` | Diverged already (the bench twin falls back to lexical's hash — recorded B5) |
| Percentile conventions | p50/p95 aggregation in `bench_scoring.py` vs the two row mappers' latency reductions | Two conventions across surfaces; root of M1's silent zeroing |
| Abstention-source plumbing | live `outcome.abstained` (bench_runner:695-713) vs rebuild `row.abstained` (`:959-965`) vs harness `output.abstained` | **Yes** — three sources, root of M17 |
| Judge-clamp endpoint sourcing | bench-run arm `cli.py:767-775` (passes `""` without `--judge-endpoint`) vs verify arm `:1270-1274` (passes configured endpoint) | **Yes** — identical settings clamp verify but not bench run (no impact today; the seam 6f899f7 was meant to freeze) |
| Primary-path vs ladder term normalization | `normalize.py:71-80` vs `zim/query.py` `_INERT_TOKENS`/`normalize_terms` | **Yes** — the two token pipelines disagree on inert words and punctuation (N39); `_INERT_TOKENS` referenced only inside zim/query.py |

---

## What checked out clean

Worth recording so future audits don't re-litigate: all ten 0822 Major fixes re-verified sound at HEAD, including the M4 rank contract across **all five** candidate emitters, M5's mirror truncation (re-sync before every attempt, `_MirrorFailure.written` carried forward), M6's per-migration `BEGIN IMMEDIATE` wrapper, and M7's lease module; `safe_gguf_basename`/`safe_zim_basename` survived a dedicated bypass hunt (unicode, `..` variants, absolute paths, backslashes, empty-after-sanitize, symlink resolve+parenthood; NUL passes the guard but fails closed at `open()`); the M9 markdown allow-list is browser-accurate and fail-closed (image tokens never render, raw HTML escaped, Shiki language allow-listed, no `{@html}` outside Shiki); SQL is parameterized everywhere traced (incl. int-coerced `PRAGMA busy_timeout`); spa.py/zim.py path traversal clean; stdlib XML fails closed on external entities (no XXE); no secrets in repo or workflows; P2's bigram cache is faithful to the naive selection; P7's hoists are real; CI runs the documented gates including pre-commit (config + dep present); `scripts/` imports nothing the dead-code sweep removed; regression-gate epsilon arithmetic is correct and fail-closed on judge parsing (`Verdict.UNJUDGED`, no lexical fallback); the vec0 store remains the healthiest subsystem.
From the fresh chat/agent-stream pass (verified against docs/sse-protocol.md and the pinned tests): happy-path event ordering on turn-1 and follow-ups, mid-stream sources-after-searching placement, merge-delta construction with continuing 0-based numbering, citation-span synthesis (n−1 mapping, out-of-range drops), window-ledger probe/reject/latch arithmetic, turn-1 pre-seed fit without history, `economy.resolve_budget`'s user→plan→default ladder with reserve clamp, tool caps/dedup/round-cap/budget steering, `_capped_read`'s focused window, `_cleanup_answer` transforms and the cleanup arm's own reset ordering, conversation_store newest-window + slim reads + trace pruning.
From the fresh answer-pipeline pass: token-economy arithmetic (ladder, reserve clamp, seed fit, ledger probe/reject/latch, window fill-in with no double-count), tokens estimator, focus.py↔passages.py parity as pinned, citation n−1 mapping and out-of-range drops, all five `answer.reformulate.*` keys wired via snapshot reads, abstention plumbing and its build-time top_score, sources_only degradation shape, `_card_to_dict` single-serializer sharing, coverage-search dedup keys, card numbering across pool merges.
From the fresh cli/config pass: settings resolution precedence (default→env→db, db wins; bootstrap env-only window deliberate), PUT /api/settings validation and single-transaction write-through, hot/cold metadata vs `_CHILD_RESTART_KEYS` (all five child-restart keys correctly cold), CLI `--set` validation, feature-flags wiring end-to-end, `_CLIIndexHandle.checkpoint` atomicity, index-lease pre-check + stranded-job supersede + 12h stale-takeover self-healing, exit codes on verify-golden/regression/compare/rejudge/matrix conflicts, the 6f899f7 consolidation holding where claimed, and live readers verified for the 9 remaining config/ declarations.
From the fresh storage/index/jobs pass: the P3 checkpoint throttle holds end-to-end (stash-before-write, flush on every terminal arm, and the ≤1s crash window is safe because resume's first-touched-batch `delete_by_article` cleanup removes partial chunks+vectors before re-insert); migration atomicity, lease claim/release atomicity with guarded release and pid-probe takeover, batch-pipeline pending-task bookkeeping, preempt duty-tracker math and recycle-vs-SIGSTOP interleavings, depth.py offset arithmetic, compat fingerprints, vector-store partition scoping/idempotent deletes/upsert transaction shapes, and db/connection WAL/busy/pool shapes all verified clean.
From the fresh frontend pass: the SSE wire parser and jobs-store consumer (shapes match `jobs/types.py` exactly, reconnect backoff correct); the reducer vs docs/sse-protocol.md on merge/reset/citations/error semantics; every `lib/api/*.ts` call cross-checked against its backend router (paths, methods, params, response shapes — all match); settings UI key lists vs backend declarations incl. the save-bar hot/non-hot split; bench UI vs the bench API contract incl. the attribution mirror; markdown pipeline beyond the recorded-clean allow-list verdict; store lifecycles; reader PDF/poll/encode paths; types.ts vs backend DTOs (omissions additive only).
From the fresh aux pass: CI green at 56aff0b running exactly the documented gates; every script's imports and call shapes resolve at HEAD; conftest isolation (no cross-test DB/event-loop leakage found); Dockerfile/compose/README mutually consistent on port 5129 and the `./data` bind; docker-publish tags correct; pyproject entry points match the CLI; PROGRESS.md Phase-24 claims match the code.
From the fresh eval-domain pass: recall@k/MRR/nDCG denominators and out-of-corpus exclusion, judge fail-closed + retry + no-cache-of-failures semantics (re-verified), weighted accuracy and calibration correlation math, dataset loader strictness outside sub_facts, article_recall arm handling, and the 6f899f7-shared metric/matrix helpers — all verified clean beyond the findings above.
From the fresh remediation-regression sweep: every in-scope fix commit verified sound at HEAD — 8126acc's unlink TOCTOU closed by the in-run lease claim, f0ade27's concurrent verify preserves golden serial order and feeds the judge clamp effective endpoints, 598d80b's merged recovery core faithful to both drivers (its one drift lands in the dead `all_messages` field), 6f899f7's runner body-identical, b599949/4d80b15/f9a9b11/1cfe5d3 unifications value-identical at every site, b66592f's estimates reach every catalog card, cdc246a/0d0ee28 pinned by tests, and the 13 dead-code removals left zero dangling references (including `ChatRequest.strategy` — pydantic ignores extras for old clients — and `zim.dir` — stale DB rows inert).
From the fresh retrieval pass: the M4 rank contract re-verified clean on all five emitters; xapian query construction cleared of escaping/operator injection beyond the inert-token gap; cross_encoder batching and static_pass feature math; dedup threshold semantics; profiles hash/load paths; and the f9a9b11 helper unification holding.

## Suggested fix order

1. **Measurement integrity (before the next benchmarking session):** M1 + M2 (one mapper pass + round-trip test), M3 + M4 (TaskGroup + in-flight guard), M5, M10 (bench driver's pre-recovery answers), M16 + M17 (headroom + abstention metrics), then carried B1/B5/B7 — the gate is only as good as these.
2. **Lifecycle:** M7 (supervisor locking), M13+M14+M15 (one runner-state fix: cooperative pause, stopping-aware cancel, row re-validation at acquire), M8 + S2 + S3 (one index-transition helper: guard, wipe sidecar, reseed), M11 (stranded-'running' reconciliation), M9 (persist accumulated sources), N1 (reuse `_parse_scope`).
3. **Security posture:** N2 (per-type param validation + A1 allowlist), N3 (secret marking + redaction).
4. **Dead controls:** M6 (decide: implement or delete), B3's six settings, N5, N4.
5. **Batch:** dead-code table, duplication consolidation (start with the two already-diverged mapper pairs), then the remaining carried mediums in the table above.
